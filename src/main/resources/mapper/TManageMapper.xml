<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.easybuy.Dao.TManageMapper">

    <select id="LoginManage" parameterType="String" resultType="TManage">
         select id,name,password from t_manage where name=#{xxx}
    </select>
</mapper>
